<%
  var {
    name,
    valueList,
  } = enumType;

  name = prefix + name;
_%>
<% if (!_.isEmpty(valueList)) { _%>
export enum <%- name %> {
<% _.forEach(valueList, (value) => {_%>
  <%-value.name %> = <%-value.number %>,
<% });_%>
}

<% } _%>
