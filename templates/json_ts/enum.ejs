<%
  var {
    name,
    valueList,
  } = enumType;

  name = prefix + name;
  var exp = prefix ? '' : 'export ';
_%>
<% if (!_.isEmpty(valueList)) { _%>
<%- exp %>enum <%- name %> {
<% _.forEach(valueList, (value) => {_%>
  <%-value.name %> = <%-value.number %>,
<% });_%>
}

<% } _%>
